<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="RatingBar.Grey" parent="@android:style/Widget.RatingBar">
        <item name="android:maxHeight">15.0dip</item>
        <item name="android:progressDrawable">@drawable/rating_bar_grey_bg</item>
        <item name="android:minHeight">15.0dip</item>
    </style>

    <style name="FullScreenTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="CartPopUpTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowTitleStyle">@style/CartPopTitleTheme</item>
    </style>

    <style name="CartPopTitleTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:background">@color/flat_clouds</item>
    </style>

</resources>
